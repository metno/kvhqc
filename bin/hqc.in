#!/bin/sh

CATCHCRASH="/usr/bin/catchcrash"
PROG="hqc"
EXE="${PROG}-@PVERSION@.bin"

if test -x "$CATCHCRASH"; then
    exec "$CATCHCRASH" --mail-to kvhqc-devel@met.no --subject "$PROG PROBLEM" --version-arg --version -- \
        "$EXE" "$@"
else
    exec "$EXE" "$@"
fi
